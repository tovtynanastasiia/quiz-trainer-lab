# Перевірка вимог до Лабораторної роботи №5: React Router

## ✅ Практично реалізовано

### 1. Можливість перейти між всіма сторінками
**Статус:** ✅ **ВИКОНАНО**

Всі сторінки доступні через навігацію:
- `/` - HomePage
- `/auth/sign-in` - SignInPage
- `/auth/sign-up` - SignUpPage
- `/auth/reset` - ResetPasswordPage
- `/quiz` - QuizPage
- `/quiz/:quizId` - QuizDetailPage (динамічний)
- `/quiz/manage` - ManageSetsPage
- `/quiz/manage/words` - ManageWordsPage (з query параметрами)
- `/account` - AccountPage (захищений)
- `/account/settings` - SettingsPage (захищений, вкладений)
- `*` - NotFoundPage (404)

**Файли:** `src/App.tsx` (рядки 52-75)

---

### 2. Сторінка 404
**Статус:** ✅ **ВИКОНАНО**

- ✅ Створено `NotFoundPage.tsx`
- ✅ Можливість повернутись на головну через `Link to="/"`
- ✅ Можливість повернутись назад через `navigate(-1)`
- ✅ Стилізовано відповідно до стилю сайту (CSS Modules)
- ✅ Адаптивний дизайн

**Файли:**
- `src/pages/NotFoundPage.tsx`
- `src/pages/NotFoundPage.module.css`

---

### 3. Використання Link замість href
**Статус:** ✅ **ВИКОНАНО**

Перевірено всі файли - **немає жодного `href` або `<a>` тега**:
- ✅ Всі посилання використовують `<Link>` компонент
- ✅ Навігація в `App.tsx` - використовує `Link`
- ✅ Всі сторінки (HomePage, QuizPage, AccountPage, тощо) - використовують `Link`
- ✅ Сторінки авторизації - використовують `Link`

**Перевірка:**
```bash
grep -r "href=" src/  # Результат: No matches found
grep -r "<a " src/    # Результат: No matches found
```

---

### 4. Захищений роут
**Статус:** ✅ **ВИКОНАНО**

- ✅ Створено компонент `ProtectedRoute`
- ✅ Використовує `Navigate` для редіректу неавторизованих користувачів
- ✅ Зберігає поточну локацію в `state` для редіректу після входу
- ✅ Застосовано до `/account` та `/account/settings`

**Файли:**
- `src/components/ProtectedRoute.tsx`
- `src/App.tsx` (рядки 64-72)

**Приклад використання:**
```tsx
<Route
  path="/account"
  element={
    <ProtectedRoute isAuthenticated={false}>
      <Outlet />
    </ProtectedRoute>
  }
>
  <Route index element={<AccountPage />} />
  <Route path="settings" element={<SettingsPage />} />
</Route>
```

---

## ✅ Теоретично (можна показати/розказати)

### 5. Отримання параметрів з URL

**Статус:** ✅ **РЕАЛІЗОВАНО**

#### Динамічні параметри (Path Parameters):
```tsx
// src/pages/quiz/QuizDetailPage.tsx
const { quizId } = useParams<{ quizId: string }>();
// URL: /quiz/123 → quizId = "123"
```

#### Query параметри:
```tsx
// src/pages/quiz/ManageWordsPage.tsx
const [searchParams] = useSearchParams();
const setId = searchParams.get("setId");
// URL: /quiz/manage/words?setId=1 → setId = "1"
```

#### State параметри:
```tsx
// src/components/ProtectedRoute.tsx
const location = useLocation();
// location.state.from - зберігається при редіректі
```

**Види параметрів:**
1. **Path Parameters** (`/quiz/:quizId`) - через `useParams()`
2. **Query Parameters** (`?setId=1`) - через `useSearchParams()` або `URLSearchParams`
3. **State** - через `useLocation().state`

---

### 6. Реалізація захищеного роуту

**Статус:** ✅ **РЕАЛІЗОВАНО**

**Файл:** `src/components/ProtectedRoute.tsx`

```tsx
const ProtectedRoute = ({ children, isAuthenticated = false }) => {
  const location = useLocation();
  
  if (!isAuthenticated) {
    return <Navigate to="/auth/sign-in" state={{ from: location }} replace />;
  }
  
  return <>{children}</>;
};
```

**Особливості:**
- Використовує `Navigate` для редіректу
- Зберігає поточну локацію в `state` для повернення після входу
- `replace` - замінює запис в історії замість додавання

---

### 7. Програмний редірект

**Статус:** ✅ **РЕАЛІЗОВАНО**

**Приклади:**

1. **Редірект на конкретний шлях:**
```tsx
// src/pages/quiz/QuizDetailPage.tsx
const navigate = useNavigate();
navigate("/quiz"); // Редірект на /quiz
```

2. **Навігація назад:**
```tsx
navigate(-1); // Назад по історії
```

3. **Редірект з заміною історії:**
```tsx
// src/components/ProtectedRoute.tsx
<Navigate to="/auth/sign-in" replace />
```

**Файли з прикладами:**
- `src/pages/quiz/QuizDetailPage.tsx` (рядки 14-20)
- `src/pages/NotFoundPage.tsx` (рядки 8-10)
- `src/components/ProtectedRoute.tsx` (рядок 17)

---

### 8. Навігація по історії переходів

**Статус:** ✅ **РЕАЛІЗОВАНО**

**Використання `navigate()` з числом:**
```tsx
const navigate = useNavigate();

navigate(-1); // Назад на 1 крок
navigate(1);  // Вперед на 1 крок
navigate(-2); // Назад на 2 кроки
```

**Приклади:**
- `src/pages/NotFoundPage.tsx` - кнопка "Назад" використовує `navigate(-1)`
- `src/pages/quiz/QuizDetailPage.tsx` - кнопка "Назад" використовує `navigate(-1)`

---

### 9. Компоненти react-router-dom

**Статус:** ✅ **ВИКОРИСТОВУЮТЬСЯ**

#### Основні компоненти:

1. **`BrowserRouter`** (як `Router`)
   - Обгортка для всього додатку
   - `src/App.tsx` (рядок 25)

2. **`Routes`** та **`Route`**
   - Визначення маршрутів
   - `src/App.tsx` (рядки 52-75)

3. **`Link`**
   - Навігація між сторінками
   - Використовується скрізь замість `<a href>`

4. **`Navigate`**
   - Програмний редірект
   - `src/components/ProtectedRoute.tsx` (рядок 17)

5. **`Outlet`**
   - Для вкладених роутів
   - `src/App.tsx` (рядок 66)

#### Hooks:

1. **`useParams()`** - параметри з URL
   - `src/pages/quiz/QuizDetailPage.tsx`

2. **`useNavigate()`** - програмна навігація
   - `src/pages/quiz/QuizDetailPage.tsx`
   - `src/pages/NotFoundPage.tsx`

3. **`useLocation()`** - поточна локація
   - `src/components/ProtectedRoute.tsx`
   - `src/pages/quiz/QuizDetailPage.tsx`

4. **`useSearchParams()`** - query параметри
   - `src/pages/quiz/ManageWordsPage.tsx`

---

### 10. Динамічні і вкладені маршрути

**Статус:** ✅ **РЕАЛІЗОВАНО**

#### Динамічні маршрути:

1. **`/quiz/:quizId`**
   ```tsx
   <Route path="/quiz/:quizId" element={<QuizDetailPage />} />
   ```
   - Використовує `useParams()` для отримання `quizId`
   - `src/pages/quiz/QuizDetailPage.tsx`

#### Вкладені маршрути:

1. **`/account` з вкладеними роутами:**
   ```tsx
   <Route path="/account" element={<ProtectedRoute><Outlet /></ProtectedRoute>}>
     <Route index element={<AccountPage />} />
     <Route path="settings" element={<SettingsPage />} />
   </Route>
   ```
   - `/account` → AccountPage (index)
   - `/account/settings` → SettingsPage
   - Використовує `Outlet` для рендерингу дочірніх роутів

**Файли:**
- `src/App.tsx` (рядки 63, 65-72)

---

## 📋 Підсумок

### ✅ Всі практичні вимоги виконані:
1. ✅ Навігація між всіма сторінками
2. ✅ Сторінка 404 з можливістю повернутись
3. ✅ Всі посилання використовують `Link`
4. ✅ Захищений роут реалізовано

### ✅ Всі теоретичні концепції реалізовані:
5. ✅ Отримання параметрів (path, query, state)
6. ✅ Захищений роут
7. ✅ Програмний редірект
8. ✅ Навігація по історії
9. ✅ Всі компоненти та hooks використовуються
10. ✅ Динамічні та вкладені маршрути

---

## 📁 Структура файлів

```
src/
├── App.tsx                    # Основна конфігурація роутів
├── components/
│   └── ProtectedRoute.tsx     # Захищений роут
└── pages/
    ├── NotFoundPage.tsx       # Сторінка 404
    ├── quiz/
    │   ├── QuizDetailPage.tsx # Динамічний роут + useParams
    │   └── ManageWordsPage.tsx # Query параметри + useSearchParams
    └── ...
```

---

## 🎯 Готово до захисту!

Всі вимоги до лабораторної роботи №5 виконані та перевірені.

