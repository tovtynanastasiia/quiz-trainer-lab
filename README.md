# Quiz Trainer - Інтерактивний Тренажер Слів

Сучасний веб-додаток для вивчення іноземних слів з використанням інтерактивних квізів. Побудований на **Next.js 14** з **Supabase** для аутентифікації та зберігання даних.

## 🚀 Основні Можливості

### 📚 Управління Наборами Слів

- Створення власних наборів слів
- Додавання слів по одному або масово через текст
- Редагування існуючих слів
- Видалення слів та наборів

### 🎯 Інтерактивне Навчання

- **Режим "Навчання"** - для вивчення нових слів з адаптивними повтореннями
- **Режим "Точність"** - тренування уважності за 4 хвилини (20 слів)
- **Режим "Швидкість"** - розвиток швидкості набору за 4 хвилини
- **Режим "Флеш-картки"** - класичний метод вивчення без текстового вводу
- **Адаптивний алгоритм** - система shortMemory (5 кроків для вивчення слова)
- **Відображення правильних відповідей** при помилках
- **Затримка показу** недавно вивчених слів
- **Система повторень** - адаптивні інтервали для кращого запам'ятовування

### 📊 Відстеження Прогресу

- Збереження прогресу в базі даних
- Статистика спроб та правильних відповідей
- Персональні налаштування акаунту
- Детальний огляд вивчених слів
- **Прогрес-бари** для візуального відображення досягнень
- **Статистика по наборах** - окремі результати для кожного набору
- **Загальна статистика** - результати по всіх наборах разом

### 🎲 Генератор Речень

- **Шаблонна система** - генерація речень на основі готових шаблонів
- **Різні рівні складності** - початківець, середній, просунутий
- **Підтримка мов** - англійська та українська
- **Категорії граматики** - present-simple, past-simple, complex, тощо
- **Налаштування кількості** - від 1 до 50 речень
- **Копіювання** - зручне копіювання всіх згенерованих речень
- **Безкоштовно** - повністю безкоштовний для всіх користувачів

### 🔐 Безпека та Аутентифікація

- Реєстрація та вхід через Supabase Auth
- Захищені маршрути для авторизованих користувачів
- Відновлення паролю
- Безпечне зберігання даних

## 🛠 Технології

### 🎨 Frontend

- **Next.js 14** - React фреймворк з App Router для сучасного веб-розробки
- **TypeScript** - Типізована JavaScript для надійності коду
- **React 18** - Бібліотека для створення інтерактивних UI компонентів
- **Tailwind CSS** - Utility-first CSS фреймворк для швидкої стилізації
- **Responsive Design** - Адаптивний дизайн для всіх пристроїв (мобільні, планшети, десктопи)

### 🗄 Backend & Database

- **Supabase** - Backend-as-a-Service платформа
  - **PostgreSQL** - Реляційна база даних для зберігання даних
  - **Supabase Auth** - Система аутентифікації та авторизації
  - **Row Level Security (RLS)** - Безпека на рівні рядків
  - **Real-time subscriptions** - Синхронізація даних в реальному часі
  - **Storage** - Файлове сховище (якщо потрібно)

### 🔧 Development Tools

- **ESLint** - Лінтер для перевірки якості коду
- **Prettier** - Форматування коду
- **TypeScript Compiler** - Компіляція TypeScript в JavaScript
- **Next.js Dev Server** - Локальний сервер розробки з hot reload

### 📦 Package Management

- **npm** - Менеджер пакетів Node.js
- **Node.js** - JavaScript runtime для серверної частини

### 🚀 Deployment & Hosting

- **Vercel** - Оптимізований хостинг для Next.js (рекомендовано)
- **GitHub Pages** - Статичний хостинг через GitHub
- **Netlify** - Альтернативний хостинг з CI/CD
- **Docker** - Контейнеризація (опціонально)

### 🔐 Security Features

- **JWT Tokens** - Безпечна аутентифікація через Supabase
- **HTTPS** - Шифрування трафіку
- **CORS** - Контроль доступу між доменами
- **Environment Variables** - Безпечне зберігання секретів

### 📱 Progressive Web App (PWA) Ready

- **Service Workers** - Кешування та офлайн функціональність
- **Web App Manifest** - Встановлення як нативний додаток
- **Responsive Images** - Оптимізація зображень для різних пристроїв

### 🎯 Performance Optimizations

- **Next.js Image Optimization** - Автоматична оптимізація зображень
- **Code Splitting** - Розділення коду для швидшого завантаження
- **Static Generation** - Генерація статичних сторінок
- **Server-Side Rendering (SSR)** - Рендеринг на сервері для SEO

### 🔄 State Management

- **React Hooks** - useState, useEffect, useMemo для локального стану
- **Context API** - Глобальний стан додатку
- **Supabase Client** - Управління станом бази даних

### 🎨 UI/UX Components

- **Custom Components** - Власні React компоненти
- **Tailwind Utilities** - Готові CSS класи для стилізації
- **Responsive Grid** - Адаптивні сітки та макети
- **Dark/Light Mode Ready** - Підготовка для темної теми

### 📊 Analytics & Monitoring

- **Supabase Analytics** - Вбудована аналітика
- **Error Tracking** - Відстеження помилок (готовність до інтеграції)
- **Performance Monitoring** - Моніторинг продуктивності

### 🌐 Internationalization (i18n) Ready

- **Multi-language Support** - Підтримка української та англійської мов
- **Localization** - Локалізація інтерфейсу
- **RTL Support** - Підтримка мов з письмом справа наліво (готовність)

### 🔧 Build & Development

- **Webpack** - Бандлер модулів (через Next.js)
- **Babel** - Транспіляція JavaScript (через Next.js)
- **PostCSS** - Обробка CSS (через Tailwind)
- **Hot Module Replacement** - Гаряча заміна модулів під час розробки

## 📦 Встановлення та Запуск

### 1. Клонування репозиторію

```bash
git clone https://github.com/your-username/quiz-trainer.git
cd quiz-trainer
```

### 2. Встановлення залежностей

```bash
npm install
```

### 3. Налаштування змінних середовища

Створіть файл `.env.local` з наступними змінними:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. Налаштування бази даних

Виконайте SQL-скрипт в Supabase SQL Editor для створення таблиць та політик RLS (див. розділ "База даних" нижче).

### 5. Запуск проекту

```bash
npm run dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

## 🗄 База Даних

### Структура таблиць:

- **sets** - набори слів користувачів
  - `id` (UUID) - унікальний ідентифікатор
  - `user_id` (UUID) - ідентифікатор користувача
  - `name` (TEXT) - назва набору
  - `description` (TEXT) - опис набору
  - `created_at` (TIMESTAMP) - дата створення

- **words** - слова в наборах
  - `id` (UUID) - унікальний ідентифікатор
  - `set_id` (UUID) - ідентифікатор набору
  - `hint` (TEXT) - підказка/слово
  - `answer` (TEXT) - відповідь/переклад
  - `created_at` (TIMESTAMP) - дата створення

- **user_progress** - прогрес вивчення слів
  - `id` (UUID) - унікальний ідентифікатор
  - `user_id` (UUID) - ідентифікатор користувача
  - `set_id` (UUID) - ідентифікатор набору
  - `word_id` (UUID) - ідентифікатор слова
  - `short_memory` (INTEGER) - рівень вивчення (0-5)
  - `updated_at` (TIMESTAMP) - дата оновлення

- **accuracy_results** - результати режиму точності
  - `id` (UUID) - унікальний ідентифікатор
  - `user_id` (UUID) - ідентифікатор користувача
  - `set_id` (UUID) - ідентифікатор набору
  - `correct_count` (INTEGER) - кількість правильних відповідей
  - `total_words` (INTEGER) - загальна кількість слів
  - `time_taken` (INTEGER) - час виконання в секундах
  - `created_at` (TIMESTAMP) - дата створення

- **speed_results** - результати режиму швидкості
  - `id` (UUID) - унікальний ідентифікатор
  - `user_id` (UUID) - ідентифікатор користувача
  - `set_id` (UUID) - ідентифікатор набору
  - `words_completed` (INTEGER) - кількість виконаних слів
  - `time_limit` (INTEGER) - ліміт часу (240 секунд)
  - `created_at` (TIMESTAMP) - дата створення

### Row Level Security (RLS):

- Всі таблиці захищені RLS політиками
- Користувачі можуть бачити тільки свої дані
- Автоматичне додавання `user_id` при створенні записів

### SQL для створення таблиць:

```sql
-- Створення таблиць та політик RLS
-- (SQL-скрипт буде надано окремо)
```

## 📱 Маршрути Додатку

### 🏠 Основні сторінки

- `/` - Головна сторінка з описом функцій
- `/sentences` - Генератор речень для тренування

### 🔐 Аутентифікація

- `/auth/sign-in` - Вхід в акаунт
- `/auth/sign-up` - Реєстрація нового користувача
- `/auth/reset` - Відновлення паролю
- `/auth/update-password` - Оновлення паролю

### 🎯 Тренування

- `/quiz` - Основний інтерфейс квізів з вибором режиму
- `/quiz/manage` - Управління наборами слів
- `/quiz/manage/words` - Додавання слів до наборів
- `/quiz/manage/[setId]` - Редагування слів в конкретному наборі

### 👤 Профіль користувача

- `/account` - Основний профіль з статистикою
- `/account/settings` - Налаштування акаунту та прогрес

## 🎨 Особливості Дизайну

### 📱 Адаптивність

- **Mobile-first** підхід до дизайну
- **Responsive breakpoints** - sm, md, lg, xl для різних екранів
- **Touch-friendly** інтерфейс для мобільних пристроїв
- **Flexible layouts** - гнучкі макети для всіх пристроїв

### 🎨 Візуальний дизайн

- **Сучасний UI** з використанням Tailwind CSS
- **Консистентна колірна схема** - сині, зелені, жовті акценти
- **Іконки та емодзі** для кращого UX
- **Градієнти та тіні** для глибини дизайну
- **Плавні анімації** та переходи

### 🧩 Компоненти

- **Переиспользуемые компоненты** - кнопки, карточки, формы
- **Модульная архитектура** - легко розширювати
- **TypeScript типи** для всіх компонентів
- **Accessibility** - підтримка скрін-рідерів

### 🎯 UX/UI Паттерни

- **Прогрес-бари** для візуалізації прогресу
- **Статистичні карточки** для відображення даних
- **Модальні вікна** для підтвердження дій
- **Навігаційні хлібні крихти** для орієнтації
- **Тултіпи та підказки** для користувачів

## 🔧 Розробка

### Структура проекту:

```
app/                           # Next.js App Router
├── auth/                     # Сторінки аутентифікації
│   ├── sign-in/             # Вхід в акаунт
│   ├── sign-up/             # Реєстрація
│   ├── reset/               # Відновлення паролю
│   └── update-password/     # Оновлення паролю
├── quiz/                     # Квіз функціональність
│   ├── manage/              # Управління наборами
│   └── manage/[setId]/      # Редагування конкретного набору
├── account/                  # Управління акаунтом
│   └── settings/            # Налаштування
├── sentences/                # Генератор речень
└── globals.css              # Глобальні стилі

components/                   # React компоненти
├── quiz/                    # Компоненти квізів
│   ├── modes/              # Режими тренування
│   │   ├── EducationMode.tsx
│   │   ├── AccuracyMode.tsx
│   │   ├── SpeedMode.tsx
│   │   └── FlashCardMode.tsx
│   ├── Layout.tsx          # Основний макет
│   └── ProgressBar.tsx     # Прогрес-бар
├── sentence-generator/      # Генератор речень
│   └── SentenceGenerator.tsx
└── auth/                    # Компоненти аутентифікації

lib/                         # Утиліти та логіка
├── quiz-data/              # Робота з даними квізів
│   └── types.ts            # TypeScript типи
├── quiz-logic/             # Логіка квізів
│   ├── shortMemory.ts      # Алгоритм адаптивних повторень
│   ├── persistence.ts      # Збереження прогресу
│   └── normalize.ts        # Нормалізація тексту
├── sentence-generator/     # Генератор речень
│   ├── types.ts            # Типи для генератора
│   ├── templates.ts        # Шаблони речень
│   └── generator.ts        # Основний генератор
├── supabase-client.ts      # Клієнт Supabase
└── utils.ts                # Допоміжні функції
```

### Основні команди:

```bash
npm run dev          # Запуск в режимі розробки
npm run build        # Збірка для продакшену
npm run start        # Запуск продакшен версії
npm run lint         # Перевірка коду
```

## 📈 Розгортання

### 🚀 Рекомендовані платформи:

- **Vercel** (рекомендовано для Next.js)
  - Автоматичне розгортання з GitHub
  - Оптимізація для Next.js
  - Безкоштовний план з обмеженнями
  - CDN та edge functions

- **Netlify**
  - Статичний хостинг
  - CI/CD з GitHub
  - Форми та функції
  - Безкоштовний план

- **GitHub Pages**
  - Статичний хостинг
  - Інтеграція з GitHub
  - Безкоштовно для публічних репозиторіїв
  - Автоматичне розгортання

### 🔧 Налаштування для розгортання:

1. **Змінні середовища** - додати в налаштуваннях платформи:

   ```
   NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

2. **Supabase налаштування**:
   - Додати домен в налаштуваннях Supabase
   - Налаштувати CORS для нового домену
   - Перевірити RLS політики

3. **Домен** (опціонально):
   - Підключити власний домен
   - Налаштувати SSL сертифікат
   - Налаштувати редиректи

### 🐳 Docker (опціонально):

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 🚀 Майбутні Можливості

### 🎯 Планується додати:

- **Google Translate API** - для кращих перекладів в генераторі речень
- **Голосове озвучування** - TTS для речень та слів
- **Темна тема** - перемикач темної/світлої теми
- **Експорт даних** - PDF/Word зі статистикою
- **Соціальні функції** - спільні набори слів
- **Мобільний додаток** - React Native версія
- **Офлайн режим** - PWA з кешуванням
- **Більше мов** - підтримка додаткових мов
- **AI рекомендації** - персоналізовані поради
- **Ігри та квести** - додаткові способи навчання

### 🔧 Технічні покращення:

- **Тестування** - Unit та Integration тести
- **Моніторинг** - Error tracking та analytics
- **Оптимізація** - Performance improvements
- **Безпека** - Додаткові заходи безпеки
- **API** - RESTful API для інтеграцій

## 🤝 Внесок

### Як внести свій внесок:

1. **Fork** репозиторій
2. **Створіть гілку** для нової функції (`git checkout -b feature/amazing-feature`)
3. **Зробіть коміт** змін (`git commit -m 'Add amazing feature'`)
4. **Push** в гілку (`git push origin feature/amazing-feature`)
5. **Відкрийте Pull Request**

### Правила внеску:

- Дотримуйтесь існуючого стилю коду
- Додавайте коментарі для складних частин
- Тестуйте зміни перед відправкою
- Оновлюйте документацію при необхідності

## 📄 Ліцензія

Цей проект розповсюджується під ліцензією **MIT**. Дивіться файл [LICENSE](LICENSE) для деталей.

## 👥 Автори

- **Neskvichok** - Основний розробник
- **Внесок спільноти** - Всі, хто допомагає розвивати проект

## 🙏 Подяки

- **Supabase** - за чудовий Backend-as-a-Service
- **Next.js** - за потужний React фреймворк
- **Tailwind CSS** - за зручний CSS фреймворк
- **Всі користувачі** - за фідбек та підтримку

---

**Створено з ❤️ для ефективного вивчення іноземних слів**

_Якщо вам подобається цей проект, поставте ⭐ зірку на GitHub!_
